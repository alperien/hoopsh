/**
 * epilogue.ts - retirement, the story, the HOF ballot, the rafters.
 * OWNER: money task. STATUS: STAGED stub; signatures frozen.
 */
import type { CareerState, Epilogue } from './types.js';

export function buildEpilogue(career: CareerState): Epilogue {
  throw new Error('career/epilogue: not implemented (money task lands this)');
}

/** Called at league season rollovers post-retirement: HOF ballots, rafters. */
export function advanceLegacy(career: CareerState): void {
  // INERT until the money task lands.
}
