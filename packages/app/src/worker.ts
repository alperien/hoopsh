/**
 * worker.ts — standalone per-process game worker. OWNER: app task.
 * STATUS: STAGED stub. Reads a job-slice JSON path from argv[2], sims
 * each job with the engine, folds via franchise foldEvents, prints ONE
 * JSON result array on stdout.
 */
throw new Error('app/worker: not implemented (app task lands this)');
export {};
