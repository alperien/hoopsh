/**
 * main.ts — `npm run gm` entry: start the server, print the URL. OWNER:
 * app task. STATUS: STAGED stub.
 */
console.error('hoopsh gm: not implemented yet (app task lands this)');
process.exit(1);
export {};
