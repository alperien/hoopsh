{
  "name": "@hoopsh/harness",
  "version": "0.2.0",
  "description": "Batch simulation runner, realism acceptance checks, benchmarks, and calibration tooling for hoopsh. Repo-internal: reads repo-root data/ and spawns workers via the repo loader \u2014 not consumable outside the monorepo.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "main": "src/index.ts",
  "dependencies": {
    "@hoopsh/engine": "*",
    "@hoopsh/stats": "*",
    "@hoopsh/data": "*",
    "@hoopsh/narration": "*"
  }
}
