# verify-career

Adversarial re-verification of career-nba.md (1-5), career-accept.md (6-10), career-week.md (11-14), career-stock.md (15-16), career-params.md (17-18). Every cited line re-read at HEAD; counter-evidence hunted in tests, REGISTER.md W66-W72, the C-register (CAREER.md C1-C15), and module headers. Pure-compute probes run (`/tmp/probe-career.ts` via tools/register.mjs: harvestSeasonHonors, accrueSeason, applyChoice, updateAfterGame — outputs quoted below). Zero engine games used.

## 1. CRITICAL epilogue ring harvest invents rings — CONFIRMED-WITH-CORRECTION
Probed: with `clock.phase='nba'`, an archive `{season: 2027, champion: 'BOS', draftClass: []}` and no 2027 ledger row minted `ev-honor-ring-2027` ("NBA champion: Celtics, 2027") — a pre-entry title harvested (P1); with phase 'china' and a ledger row `'Sharks, contract year 2032'`, BOS's 2032 title also minted (P2) — the champion check (epilogue.ts:36) uses CURRENT `career.nbaTeam`, never the team-of-that-season. Guard 1 (epilogue.ts:54 `|| career.clock.phase === 'nba'`) is unconditionally true at every active-NBA year wrap (tick.ts:486), and guard 3 is vacuous: `archiveSeason` (almanac.ts:124-141) always writes `draftClass` as an array — empty, in fact, since it runs at the finals→lottery transition before that season's draft transactions exist. CORRECTION: the substring sub-claim ("season 3 matches contract year 13/23/30…, near-always true") is inapplicable at HEAD — seasons are 4-digit (creation.ts START_YEAR 2026), so `label.includes(String(season))` behaves as exact-season membership (P3 showed a non-matching season NOT granted); the fallback's real defect is the missing team check, and the vacuous phase disjunct carries the headline anyway. Severity CRITICAL stands: 12 legacy points per phantom ring vs hofScoreFloor 70 / jerseyRetireScore 55 (params.ts:305-307), directly falsifying epilogue.ts:5-6 and CAREER_INTERNALS.md's epilogue row; no C-register or W-row covers it.

## 2. CRITICAL Euro descent vet funnels to draftPrep and is RE-DRAFTED — CONFIRMED
Every chain link verified at HEAD: `applyAbroadOffer` sets `career.nbaTeam = null` and phase euro (nbabridge.ts:874-876) and binds me into BOTH pools (nbabridge.ts:885 `career.players[career.me] = me`); the tick.ts:397 guard `(euro||nbl) && nbaTeam===null && !epilogue` cannot distinguish the vet and moves him to draftPrep; at draftWeek `resolveDraftNight` calls `enterDraftClass`, whose `enter(career.me)` finds him in `career.players`, flips status to 'draftEligible', and pushes him into `league.draftClass` (stock.ts:794-798) — the `alreadyIn` check (stock.ts:801,809) only suppresses the STORY event, not the entry, so career-week.md's rival account ("enterDraftClass no-ops… posts undrafted") is WRONG on this point. `availableProspects` (franchise tick.ts:128) then boards him, `aiSelect` prices his high current ability (draftai.ts:56-63), and `executeDraftSelection` (transactions.ts:223) overwrites `player.draft` and signs rookie-scale/2-yr-minimum. The age-40 branch (tick.ts:404) is unreachable for euro/nbl descent vets (the :397 branch returns first), retire is denied in euro (tick.ts:181), and china is untouched by :397 — the doc's symmetric forks (CAREER.md:324-333) diverge exactly as filed. CRITICAL stands.

## 3. HIGH retire leaves me on the roster; ghost seasons harvest honors — CONFIRMED-WITH-CORRECTION
`applyChoice('retire')` (tick.ts:180-187) sets phase and builds the epilogue, touching no league state; franchise retirement rolls skip careerControlled (retire.ts:116), the FA market never signs them (fa.ts:284), and the retired loop advances whole league seasons on fastSim (tick.ts:433-436), which generates lines for the top 8 of every roster (fastsim.ts:26-42) — the ghost keeps playing and awards voting reads those season rows (awards.ts:22). The award branch has no season gating: probe P4 harvested a post-retirement MVP ("MVP, 2044") into honors, and `harvestSeasonHonors` runs every retired-year advance (tick.ts:440), so legacyScore at the rafters/ballot checks (advanceLegacy) reads a posthumously growing resume. CORRECTION: the "rings branch stays armed" sub-claim is inert for NEW post-retirement titles — with phase 'retired' the phase disjunct is false and the ledger (frozen after retirement; accrueSeason not called in the retired loop) contains no post-retirement season numbers, so P3 granted nothing; only the award branch mints new honors. HIGH stands on the roster-ghost + ungated awards alone.

## 4. HIGH money.ts (year,label) dedupe double-counts and skips — CONFIRMED
Both failure modes reproduced by probe. P5 (double-count): a signing row `(year 2031, 'Celtics, contract year 2031')` plus a wrap at league.season 2030 (fallback `?? c.years[0]!`, money.ts:61 — deduped, same career year) followed by a wrap at league.season 2031 booked 'contract year 2031' AGAIN at career year 2032 — `already` (money.ts:33) requires `e.year === year`, so ledger held two rows for one contract season. P6 (skip): when the league rolled twice between wraps (2030→2032), the 2031 contract line was never accrued. The signing rows are written exactly as the header's dedupe design says (nbabridge.ts:731/755/833 + header :83-90), so the header's guarantee is falsified under the drift W67(f) itself registers; frequency claims (~5-15%/deal, double-roll every ~6 years) are consistent with the 364-vs-313-day arithmetic. HIGH stands (careerEarnings, epilogue nbaSeasons, legacy earnings term).

## 5. HIGH energy is a dead meter in the NBA phase — CONFIRMED
Grep confirms `applyLegs` has exactly one consumer, the circuits ME projection (circuits.ts:631); `advanceMyGameDay` projects with `applyApproach` only (nbabridge.ts:357), which never reads energy. The career-side energy-scaled hazard lives only in `rollMyInjury` (week.ts:172-174), module-private to resolveWeek (pre-NBA/descent phases); NBA injuries ride franchise `rollPostGameInjuries`/`advanceRecoveries`, which cannot read `career.energy` (franchise has no career dependency). So post-draft, energy is drained (nbabridge.ts:413) and restored (resolveAllocation) but consumed by nothing — while CAREER.md:179-180 claims "energy debt raises injury hazard through the existing model" and params.ts:67-73 memorializes the identical regression ("41 weeks at 0 energy cost nothing measurable") that this reintroduces in the flagship phase. C13-C15 and W67 register nothing about it. HIGH stands.

## 6. CRITICAL reacting-world gate tautological — CONFIRMED
Attempted to break it and could not. The ladder chain (trust.ts:196-238) is exhaustive over `clock >= reactGames`: promote (idx<top), demote (idx>0), at-ceiling (idx===top, resets :229), at-floor (idx===0, resets :235); at most one clock is nonzero (each increment zeroes the other, :183/:186), RoleId's union is exactly the 7 LADDER entries (types.ts:171, closing the idx=-1 loophole), roleClock has one writer plus three zeroing initializers, nothing throws between increment and ladder, and the DNP early-return (:134-141) exits before the increment. Probe P8 drove 26 consecutive above-band games: max clock observed after any call = 5 (reactGames 6), role climbed bench→rotation→sixthMan→starter→featured — the harness check (career-acceptance.ts:199-202) is unreachable at any reactGames >= 1. The gate also cannot see the real failure modes filed (dropped role move with kept reset; grading skips at week.ts:243 / nbabridge.ts:407; legal wipes at tick.ts:319 / nbabridge.ts:285). CRITICAL per the "gate that cannot fail" severity convention.

## 7. HIGH completion gate blind outside hs/college — CONFIRMED
career-acceptance.ts:289 flags only `finalPhase === 'hs' || 'college'`. `transitionAtYearWrap` (tick.ts:326-412) has no draftPrep arm, `resolveDraftNight`'s undrafted branch leaves phase unchanged (tick.ts:261-264), the age-40 forced retirement covers only china/euro/nbl (:404), and `retire` is denied outside nba/china (:181) — so an undrafted pilot loops draftPrep to MAX_WEEKS and passes gate 1, contradicting CAREER.md's "Careers complete: no stage soft-locks". The NaN `--nbaSeasons` pin also verifies: `nbaWeeksLived >= NaN*52` is always false (line 173), so retire never fires and finalPhase 'nba' passes. Fair nuance the finding itself states: `applyNbaOffer` has no phase gate and the two-way floor guarantees a door, so a human can always exit — the harness script cannot. HIGH stands.

## 8. HIGH determinism gate is 40 HS weeks, never replays choiceLog — CONFIRMED
career-acceptance.ts:240-260: creates at week 10 (creation.ts:601 starts the clock at hsSeasonStartWeek), advances 40 weeks to week 50 — below weeksPerYear 52, so no year wrap, no accrueSeason/harvest/transition, no college/draft/NBA/epilogue ever enters the byte compare; and it re-drives the same script twice rather than replaying a recorded `career.choiceLog`. The claims it is held against are stronger: header lines 15-16, CAREER.md ("same seed plus same choice log replays the identical career, byte for byte") and REGISTER W66 ("byte determinism of (seed, choiceLog) replays"); only CAREER_INTERNALS.md:133-135 states the honest scope ("a 40-week scripted career"). HIGH stands.

## 9. HIGH lint sees only surviving rows — CONFIRMED
The lint (career-acceptance.ts:209-217) reads events, `career.coach.grades`, and ledger; CoachState is replaced wholesale with `grades: []` at every bench change (tick.ts installNextCoach :311-320; nbabridge freshCoach :274-286, fired on draft entry, trades, coachChange, market signings), so at career end only the final tenure's grades exist to lint. `career.stock.history` rows carry reasons (small moves are deliberately history-only, stock.ts:688-690) but are never read, and week.ts:144 mutates morale with no event at all — while CAREER.md claims "every trust, morale, and stock delta carries a stated reason". HIGH stands.

## 10. HIGH three documented bands absent; euro/nbl unreachable for pilots — CONFIRMED
The header (career-acceptance.ts:18-21) and CAREER_INTERNALS.md:137-139 both list a "career shapes by phase" band; the code (:292-310) computes draft ordering, content pulse, zero-event streak, energy floor, and phone volume — no shapes band, no route-outcomes band (CAREER.md:437-441), and the boredom audit as defined (consequential DECISION per two weeks; phone-mix uniqueness) is replaced by a weaker all-events-per-week metric. The pilots issue only setWeekPlan/setApproach/declareDraft/attendWorkout/retire, so commitments flow through the auto-fallback that sorts college offers first (tick.ts:339-341). One precision note: euro/NBL route offers DO exist on the HS board (recruiting.ts:500-529, ceiling-gated) and the fallback could technically pick one if every college interest closed while a pro offer sat open — a near-contradictory edge (pro offers require top-30 ceilings that keep college doors warm; W67(h)'s fleet went all-college), so the substance ("cannot execute for any pilot") holds in practice. HIGH stands.

## 11. HIGH applyChoice TypeError on malformed payloads — CONFIRMED-WITH-CORRECTION
Probe re-run (P7): `{kind:'setWeekPlan'}`, `{plan:null}`, `{kind:'setApproach'}`, `{card:null}` all threw TypeError (tick.ts:85-86 reads `choice.plan.slots`; validCard at :60 indexes the card), while respondPhone/contractDecision denied gracefully — falsifying the module contract (tick.ts:76-77 "never throws for a bad input") and CAREER_INTERNALS.md:37. The payload does arrive over the network with only a truthiness check (server.ts:305-310). CORRECTION: the finding's "unhandled throw" overstates the blast radius — `createServer`'s top-level `.catch` (server.ts:728-732) converts it to an HTTP 500, so no process crash and (validation-first) no corruption; the broken documented guarantee and the 500 on the two most common choice kinds keep this HIGH-defensible, MEDIUM arguable.

## 12. HIGH playing hurt unreachable; healthy flag-setter skips injury roll — CONFIRMED
Circuits: `meListed` (circuits.ts:618-624) sits a listed me unconditionally, no playingHurt override, so `played && me.health.injury` is impossible there (sole live edge: a game-1 injury reaching game 2's wear-compounding at week.ts:251-254, since the week's jobs pre-date the roll). NBA: nbabridge.ts:28-32 registers that franchise availability (gameday healthyPool, :73) sits me and "v1 does not fight it". Meanwhile tick.ts:102 accepts the flag with no listing check, and week.ts:175 `if (playingHurt) return;` sits BEFORE the rng draw (:177) — a healthy flag-setter pays the whole-sheet debuff (approach.ts:87-94) plus +1.1 wear/game (playHurtWearMult 2.2 × 0.5) and skips his post-game injury roll entirely, the backwards half. Contradicts CAREER.md's v1-in-scope list (:386-387 "playing-hurt choices on the real wear model"); only the nbabridge header registers its half. HIGH stands.

## 13. MEDIUM post-entry injuries heal 14 days/week — CONFIRMED
resolveWeek decrements `remainingDays -= 7` (week.ts:219-225) on the object read from either pool (:216), and every non-NBA week also runs `fastDays(7)` (tick.ts:471-474), whose advanceDay calls `advanceRecoveries` — `-1` per day for everyone in league.players, no careerControlled exclusion (injury.ts:167-182). Post-entry I am in league.players in exactly the affected phases: china/euro descent (nbabridge.ts:885 binds ONE object into both pools) and undrafted draftPrep (enterDraftClass moved me there). Pre-entry is clean (absent from league.players); the NBA phase is clean (resolveNbaWeek has no -7). MEDIUM stands.

## 14. MEDIUM off-season energy dominant strategy — CONFIRMED
All three energy consumers are game-gated: the hazard multiplier fires only inside the post-game roll (week.ts:172-174), the legs tax only inside the circuit game projection (circuits.ts:631), and gameEnergyCost only on played games — a gameless week at energy 0 costs nothing. Arithmetic checks against params (weekBaseRecovery 30; costs practice 12, extraWork 16, rest -25): triple-extraWork = 30-12-48 = -30/week clamped at 0 with no debt carried (week.ts:148); triple-rest = +93/week restores 0→100 in two weeks; and `accrueTraining` never reads energy, so the 3×0.16×staffF ≈ 0.45 pts/week accrues at zero cost vs 0.30 for the "responsible" plan. The floor event (:149-151) is text only. MEDIUM stands.

## 15. HIGH combine shock/reprice unreachable — CONFIRMED
Structurally decisive at HEAD: `combineThisWeek = stock.combineDone && week === combineWeek` (stock.ts:601), but tick runs resolveWeek→updateStock (tick.ts:459, week.ts:278) BEFORE runCombineWeek (:462-465) and increments the week after (:478) — at week 33 combineDone is false, from week 34 the equality fails, so the shock cap (shockMoveCap 14 vs weeklyMoveCap 6) never arms for the combine and the reprice reason branch (stock.ts:653-656) is dead; feed hygiene (:688-690) treats the real reprice week as non-shock, contradicting the header's "shocks always print". The one satisfiable state is the zombie: combineDone true from a prior year plus week 33 of a later undrafted-draftPrep year (STOCK_PHASES includes draftPrep, :143) — a phantom shock a year late. stock.test.ts:193-195 uses the same broken order and asserts shockMoveCap only as an injury-week bound (:161), never the combine path. HIGH stands.

## 16. HIGH fog handoff severed — CONFIRMED
The stream families are disjoint by construction: the career mirror draws `career-scout:*`/`career-scout-bias:*` under career.seed (perception.ts:82-91) while franchise scouting draws `scout:<teamId>:<pid>`/`scout:<teamId>:<scoutSeed>` under the league seed `${seed}:world` (creation.ts:588; scouting.ts:84-110) — at enterDraftClass all thirty rooms re-roll from scratch. No career code writes `league.scouting` (grep: zero hits), franchise `runCombine` fires at the lottery→draft transition (franchise tick.ts:939) before enterDraftClass appends me, so the user chair reads me at coverage 0 (sd 9) and AI rooms at flat combineCoverage 15 (sd 8.25, params 9/4/15 verified) — while the pre-entry mirror's coverageFor (stock.ts:340-358) reaches ~80-100 with the combine/workout bumps. aiSelect prices the pick from those fresh wide reads only (draftai.ts:56-63), so workout attendance and the sharp final mock (BOARD_BLEND_FINAL 0.92) are display-only for the actual selection. W67(h) registers generous draft depth, not this severance. HIGH stands.

## 17. HIGH PREP inherits 5 NBA-only rule fields — CONFIRMED
PREP spreads NBA (packs.ts:24) and overrides every RulePack field EXCEPT exactly the five named: teamFoulBonusAtOT (inherits 4), lateWindowSec (120), lateWindowFoulBonusAt (2), makeStopClockFinalSec (120), makeStopClockEarlySec (60) — all live (bonusFreeThrowAward reads the OT threshold at rulepack.ts:370 and the late window at :381-388; shooting.ts:235-242 consumes the make-stops). The interface doc explicitly instructs carry leagues to set the OT threshold equal to teamFoulBonusAt (rulepack.ts:90-96), and PREP's own `teamFoulsCarryToOT: true` plus the engine's carry behavior (possession.ts:766) means a carried Q4 count ≥ 4 puts HS overtimes in the bonus from the first whistle; NFHS has no last-2:00 take-foul penalty and no made-basket clock stop. NCAA and EUROLEAGUE in the same import chain set all five explicitly with "audit M-11 class" comments — the exemplar was on screen. Falsifies packs.ts:7-8 ("Structural values are REAL rule-book numbers"); C9 covers statistical calibration, not rule truth, so no register row shields it. HIGH stands.

## 18. MEDIUM CBA_CHINA 4x10 vs real 4x12; PREP bonus matches no NFHS era — CONFIRMED
CBA_CHINA is a bare EUROLEAGUE spread (packs.ts:70) inheriting periodMinutes 10 (rulepack.ts:298); the real Chinese CBA plays 4x12-minute quarters (48-minute games, a real driver of its gaudy stat lines — exactly the texture CAREER.md's China fork sells), so the packs.ts:66-69 comment is factually wrong and `periodMinutes: 12` is the one-field fix. Cadence check as instructed: the engine counts team fouls PER PERIOD (`teamFoulsPeriod` reset each period unless the OT-carry rule, possession.ts:766; bonusFreeThrowAward takes teamFoulsInPeriod), and PREP has periods=4, so its 7/10 one-and-one/double thresholds apply per 8-minute QUARTER — the pre-2023 NFHS book counted 7/10 per HALF, and the current (2023-24+) book is a flat two at the FIFTH team foul per quarter with the one-and-one eliminated: the shipped hybrid matches no NFHS era and under-awards bonus trips relative to both. The shotClockOffRebSec 20 sub-claim also holds (20 is the NCAA reset; NFHS shot-clock guidance resets to the full 35). MEDIUM stands (NCAA's periods-are-halves shape makes the same engine counting correct there, as the finding notes).

## Summary

| # | file | finding | verdict |
|---|------|---------|---------|
| 1 | career-nba | epilogue ring harvest invents rings | CONFIRMED-WITH-CORRECTION (substring sub-claim inapplicable at 4-digit seasons; phase-guard + no-team-check carry it; CRITICAL stands) |
| 2 | career-nba | Euro descent vet re-drafted | CONFIRMED (career-week.md's "enterDraftClass no-ops" counter-account is wrong) |
| 3 | career-nba | retire leaves roster ghost harvesting honors | CONFIRMED-WITH-CORRECTION (post-retirement RING minting is inert; awards branch carries it; HIGH stands) |
| 4 | career-nba | money dedupe double-count/skip | CONFIRMED (both modes probed) |
| 5 | career-nba | energy dead in NBA phase | CONFIRMED |
| 6 | career-accept | reacting-world gate tautological | CONFIRMED (probe: clock never survives a call at reactGames) |
| 7 | career-accept | completion gate blind outside hs/college | CONFIRMED |
| 8 | career-accept | determinism gate 40 HS weeks, no choiceLog replay | CONFIRMED |
| 9 | career-accept | lint sees only surviving rows | CONFIRMED |
| 10 | career-accept | three documented bands absent; euro/nbl unreachable | CONFIRMED (theoretical fallback-euro edge noted; substance holds) |
| 11 | career-week | applyChoice TypeError on malformed payloads | CONFIRMED-WITH-CORRECTION (server top-level catch → 500, not a process crash) |
| 12 | career-week | playing hurt unreachable; flag skips injury roll | CONFIRMED |
| 13 | career-week | post-entry injuries heal 14 days/week | CONFIRMED |
| 14 | career-week | off-season energy dominant strategy | CONFIRMED |
| 15 | career-stock | combine shock/reprice unreachable | CONFIRMED |
| 16 | career-stock | fog handoff severed | CONFIRMED |
| 17 | career-params | PREP inherits 5 NBA-only rule fields | CONFIRMED |
| 18 | career-params | CBA 4x10 vs 4x12; PREP bonus era hybrid | CONFIRMED |

Tally: 15 CONFIRMED, 3 CONFIRMED-WITH-CORRECTION, 0 OVERSTATED, 0 WRONG, 0 KNOWN-DEBT. No REGISTER.md row or C-register cut covers any of the eighteen; one cross-reviewer conflict resolved (item 2, against career-week.md's account).
