# career-nba
scanned: packages/career/src/nbabridge.ts (927), packages/career/src/money.ts (67), packages/career/src/epilogue.ts (139), packages/career/test/nbabridge.test.ts (250). Context read for verification: career types.ts/params.ts/approach.ts/trust.ts/week.ts/tick.ts/stock.ts/fastsim.ts/circuits.ts (projection block)/phone.ts (promise block); franchise tick.ts, ai/fa.ts, ai/roster.ts, ai/valuation.ts, people/dev.ts, people/disposition.ts, transactions.ts, cba/cap.ts, cba/contracts.ts, rng.ts, media/almanac.ts; app saves.ts, career-acceptance.ts; docs/CAREER.md, docs/CAREER_INTERNALS.md.
critical: 2   high: 3   medium: 6   low: 8

## CRITICAL packages/career/src/epilogue.ts:37 - The ring harvest invents rings: every gate in the "was I there" check is vacuous
`harvestSeasonHonors` grants a championship for ANY archived season where `archive.champion === career.nbaTeam` — my CURRENT team, not the team I played for in that season. The three guards all fail:
1. `wasMySeason` (epilogue.ts:52-55) returns `true` unconditionally while `career.clock.phase === 'nba'` — which is every year-wrap harvest of an active NBA career.
2. Its ledger fallback (epilogue.ts:53) is a SUBSTRING match: `e.label.includes(String(season))`, so season 3 matches "contract year 13/23/30…"; with small genesis season numbers it is near-always true, and it never checks which TEAM the contract year was with.
3. `archive.draftClass !== undefined` (epilogue.ts:38, "a fully stamped archive") is vacuous: `archiveSeason` (franchise/src/media/almanac.ts:141) always writes `draftClass` as an array (empty at the finals horn), never undefined.
Consequence: sign with or get drafted by any franchise that won a title in ANY archived season — including the 5+ pre-entry fast-sim seasons and seasons I spent on other teams — and the first year wrap pushes `ev-honor-ring-<season>` ("NBA champion: …"). Each phantom ring is worth 12 legacy points (epilogue.ts:61), the largest single term against `hofScoreFloor` 70 and `jerseyRetireScore` 55. Over a career, the odds my current team owns some archived title are large. Directly falsifies the module header ("nothing here invents a resume", epilogue.ts:5-6) and docs/CAREER_INTERNALS.md:41 ("rings and awards read the ballots — nothing invents a resume").
Breaks: the epilogue, rings count, legacy score, HOF induction, and rafters in the career save — silently, at high frequency.

## CRITICAL packages/career/src/tick.ts:397 + packages/career/src/nbabridge.ts:874-876 - The Euro descent door routes a veteran back into the draft
`applyAbroadOffer` (nbabridge.ts:874-876) sets `career.nbaTeam = null` and `career.clock.phase = 'euro'` for a descent vet taking the Europe fork. The year-wrap transition `if ((phase === 'euro' || phase === 'nbl') && career.nbaTeam === null && !career.epilogue)` (tick.ts:397) cannot distinguish him from a pre-draft showcase teenager — the state shapes are identical — so after ONE euro season the 32-38-year-old is moved to `draftPrep` ("the showcase year is over; automatically eligible"). He then runs the combine (tick.ts:463-465), and at draftWeek `resolveDraftNight` → `enterDraftClass` (stock.ts:792-799) flips his status to `draftEligible` (he is in `career.players` because nbabridge.ts:885 put him there), and `processDraft` puts a high-current-ability veteran on the AI boards. If selected, `executeDraftSelection` (franchise/src/transactions.ts:222) OVERWRITES `player.draft` — erasing his real draft history — and signs him to a fresh rookie-scale contract at ~34. The `age >= 40` retirement branch (tick.ts:404) is unreachable for euro because the draftPrep branch returns first. The China door is unaffected (not in the line-397 condition), so the two abroad forks that docs/CAREER.md:324-333 present as symmetric descent options behave completely differently. Reached by exactly the designed use: accept `abroad:euro:<season>` and play the season out.
Breaks: the career save's arc and draft record (silently overwritten); the documented descent design (docs/CAREER.md stage 4; nbabridge.ts:72-79 offer conventions).

## HIGH packages/career/src/tick.ts:180-187 + packages/career/src/epilogue.ts:43-47 - Retiring does not remove me from the league: ghost seasons keep harvesting honors
`applyChoice('retire')` sets `career.clock.phase = 'retired'` and builds the epilogue but touches NO league state: no `executeWaive`, no `executeRetirement`. A player retiring mid-contract stays on his NBA roster; the retired-phase loop (tick.ts:434-436) then advances whole league seasons on `fastSim`, which generates box lines for everyone on a roster (fastsim.ts:27-41) — so the "retired" player keeps playing, keeps accruing season rows, and can keep winning awards (fastSim lines feed the real ballots). Franchise-side cleanup never comes: retirement rolls skip careerControlled players (franchise/src/people/retire.ts:116) and the FA market never signs them (ai/fa.ts:284), so the ghost plays until his contract years shed. Each retired-year tick calls `harvestSeasonHonors` (tick.ts:440), whose award branch (epilogue.ts:43-47) has NO season gating at all — any post-retirement award with `career.me` in `winners` harvests straight into the honors list — and the ring branch stays armed because `career.nbaTeam` is never cleared. `legacyScore` is recomputed at the rafters (since==2) and ballot (since==4) checks, so the HOF decision reads the inflated, posthumously-growing resume.
Breaks: "nothing invents a resume" (epilogue.ts:5-6, docs/CAREER_INTERNALS.md:41); the epilogue, legacy score, HOF and rafters; the almanac shows a retired player still playing.

## HIGH packages/career/src/money.ts:33,61 - The (career-year, label) accrual dedupe fails under the two-clock drift: first contract years double-count, drift years skip
The dedupe `already(label)` requires `e.year === year` — the CAREER year — but the same CONTRACT year can legitimately be recorded at two different career years. Concrete double-count: sign any deal in the league offseason (signingSeason = N+1; applyNbaOffer/QO/extension record "Team, contract year N+1" at career year Y via nbabridge.ts:731/755/833, per the header's dedupe design nbabridge.ts:83-90). If career year Y wraps BEFORE the league rolls to N+1, accrueSeason's `find(y.season === league.season)` misses and the `?? c.years[0]!` fallback (money.ts:61) books "contract year N+1" — deduped (same year Y). But the NEXT wrap (Y+1) sees `league.season === N+1`, `find` hits `years[0]`, and `already` compares `e.year === Y+1` against the signing row's Y: NOT deduped — contract year N+1 is recorded twice. Because the league's rollover position drifts uniformly through the career year (313-day seasons vs 364-day years, docs/CAREER_INTERNALS.md "The two clocks"), a meaningful fraction of offseason signings hit this (per-deal ~5-15%; several deals plus extensions per career). The reverse defect: when the league rolls TWICE inside one career year (every ~6-7 years by the registered drift), the single wrap samples only the current `league.season`, and the skipped season's contract line is never accrued.
Breaks: `careerEarnings`, the epilogue's earnings and `nbaSeasons` count, the legacy score's earnings term, and `wasMySeason`'s ledger evidence — the header's explicit guarantee that "the (year, label) dedupe suppresses a double count" (nbabridge.ts:85-87) is false under the drift the module itself documents. Dedupe on the label alone (it already encodes team + league season) would fix the double-count.

## HIGH packages/career/src/nbabridge.ts:353-367 - Energy is a dead meter in the NBA phase: no legs debuff, no hazard multiplier, doc claims otherwise
`advanceMyGameDay` projects with `applyApproach` only — `applyLegs` (the energy-to-attributes debuff added by a measured felt-loop A/B precisely because "41 weeks at 0 energy cost nothing measurable", params.ts:67-76) is consumed ONLY by the circuits projection (circuits.ts:631; grep confirms no other consumer). NBA-phase injuries ride the franchise system natively (header nbabridge.ts:25-28), and `rollPostGameInjuries` cannot read `career.energy`, while the career-side energy-scaled hazard (week.ts:172-174) is pre-NBA only. Net: in the flagship phase, `career.energy` is drained (nbabridge.ts:413) and restored (resolveAllocation) but consumed by NOTHING — no box-score cost, no injury cost, only the "running on empty" event text (week.ts:150). docs/CAREER.md:179-180 states "Overtraining is allowed and it hurts: energy debt raises injury hazard through the existing model" — false after draft night, where most of a career is lived. Rest-vs-grind, the core of the allocation turn (CAREER.md "A week in the life"), is strictly dominated by grinding in the NBA, the exact regression the felt-loop fix repaired for circuits.
Breaks: a documented mechanic in the mode's longest phase; the week economy's decision surface (grinding is free); nothing registers the gap (C13-15 don't cover it).

## MEDIUM packages/career/src/nbabridge.ts:612-624 - buildMyOffers has no team-need term; header, internals row, and its own JSDoc claim one
docs/CAREER_INTERNALS.md:39 says "buildMyOffers (cap room + team need; abroad doors when interest thins)"; the function's JSDoc says "teams with room and a need offer near my fair value" (nbabridge.ts:581-582). The code sorts candidates by `spaceWithHolds` alone (nbabridge.ts:614-617) and takes the top two; the in-loop comment "(need is priced into the jitter band)" (nbabridge.ts:613) is a fig leaf — `jitterA`/`jitterB` are pure uniform draws unrelated to roster composition. The real market machinery has an actual `needScore` (franchise/src/ai/fa.ts:157-172: positional stock, roster floor, timeline fit) that gates every AI bid on every OTHER free agent; my market ignores it, so the richest-room team offers me a rotation deal even with four bodies at my position, and a positionally-starved contender never surfaces above its hardcoded minimum call.
Breaks: the doc-vs-code claim in two places plus the module's own comment; offer realism diverges from the machinery that prices every other player in the same market.

## MEDIUM packages/career/src/nbabridge.ts:918-925 - The trade-request morale cost is erased within one week by the franchise morale recompute
`setTradeRequest` applies `requestMoraleCost` (params.ts:173-174: "trade-request morale penalty while pending") directly to `me.morale`. But franchise `updateDispositions` runs every 7th league day (franchise/src/tick.ts:869-871) and does a STATELESS recompute — `player.morale = moraleFor(league, player)` (people/disposition.ts:185, header: "morale is a function of today's league state") — which knows nothing about the career-side request. The −10 evaporates at the next weekly pulse, so "the ask weighs" (the pushed morale event, pillar 2) describes a state that does not persist; the same erasure hits week.ts's life-slot morale gains in the NBA phase. Secondary defect: the clamp asymmetry lets ask/withdraw cycles farm morale at the floor — morale 5 → ask → clamp to 0 → withdraw → +10 → 10 > 5 (nbabridge.ts:918 vs 923).
Breaks: the module's only stated v1 consequence for trade requests (header nbabridge.ts:91-96); an explained-consequence event whose consequence silently un-happens.

## MEDIUM packages/career/src/nbabridge.ts:697-708 - A player option answered after the league rollover truncates a live contract year: zombie contract
`detectContractWindows` surfaces `option:<season>` during offseason phases (nbabridge.ts:440-450) with "silence rides as opting in" and no deadline; `applyContractDecision` validates only that a `playerOption` year for that season EXISTS (nbabridge.ts:699-701) — it never checks that the season is still in the future. Because careerControlled skips keep the option unresolved franchise-side (franchise/src/tick.ts:413, ai/fa.ts:400), the flag survives the rollover. Answering 'decline' after the league rolls into the option season calls `executeOptionDecision`, which splices from the option year (transactions.ts:264-265) — now `years[0]` — leaving `contract.years = []`: a non-null contract with zero years. `releaseExpiredContracts` (franchise/src/tick.ts:339-341) can never release it (`shed.length === 0` → continue), so I stay status 'roster', unpaid, off the FA market (`buildMyOffers` requires 'freeAgent', nbabridge.ts:591), indefinitely. The window is real: the option event fires up to ~8 career weeks before rollover and the phone imposes no deadline on it.
Breaks: league cap/roster state and the career's market access; a surfaced UI path produces an unreachable-state save.

## MEDIUM packages/career/test/nbabridge.test.ts:118-128 - The swap-leak test proves only the no-op direction; the loss-free reconcile and the money/epilogue modules are untested
The test asserts object identity and byte-identical `tend` after weeks in which nothing but the swap touched me — it cannot fail unless the card itself leaks. Untested: (a) an in-day attr/tend delta (the all-star `runDevelopmentReview`, the header's stated reason the delta reconcile exists, nbabridge.ts:20-23) surviving onto the original; (b) attr restoration under `playingHurt`; (c) scalar reconciliation (morale/status/contract/rights) after an in-day waive/option/signing; (d) `detectContractWindows`' once-per-window dedupe (never constructed in any test). Meanwhile money.ts and epilogue.ts have ZERO unit tests (no money.test.ts/epilogue.test.ts; grep finds no accrueSeason/legacyScore/harvestSeasonHonors/advanceLegacy in tests), and the acceptance harness only lints ledger labels for non-emptiness and reports earnings as a band (career-acceptance.ts:215-235) — every defect filed above in that cluster sails through it.
Breaks: the verification claim in docs/CAREER_INTERNALS.md:124 ("nbabridge (the swap leak check, offers, determinism)") covers materially less than the header's "loss-free and leak-free both ways" (nbabridge.ts:23).

## MEDIUM packages/career/src/epilogue.ts:62-63 - Finals MVP is double-counted in the legacy score via substring matching
`count('MVP,')` matches every honor reason containing "MVP," — which includes "Finals MVP, <season>" (AWARD_LABELS fmvp → 'Finals MVP', reason format epilogue.ts:45). Every Finals MVP therefore scores 15 (as an MVP) + 8 (as an FMVP) = 23 instead of 8. Against `hofScoreFloor` 70 and `jerseyRetireScore` 55 a single ring run (12 + 23) moves the borderline-band arithmetic (epilogue.ts:116-119) by a full spurious MVP.
Breaks: legacy score math; HOF and rafters marginal decisions; compounds with the phantom-ring inflation above.

## MEDIUM packages/career/src/trust.ts:33-35 + docs/CAREER.md:395-414 (C13) - Verified: no code path prices NBA minutes off career role — but the par coupling makes the ladder a follower of the rotation, which the register does not state
Verified as the register claims: `minutesByRole` is consumed only by the circuits projection (circuits.ts:683; grep confirms), the NBA rotation is franchise-owned, and nothing career-side writes `team.rotation`. What a role change DOES change in the NBA: `planFor` latitude (approach.ts:163-183), the grading par (`ROLE_PAR`, trust.ts:33-35), trust math, and phone/coach copy — label, latitude, and text only. The unregistered second-order cost: promotion RAISES par while franchise minutes hold production constant, so `roleClock.above` streaks structurally end one rung above whatever the rotation's minutes can produce (promoteAt 68 needs composite ≈ par + 8.2 six games running; ~24 franchise minutes ≈ composite 19, unable to clear starter par 16 + 8.2). The ladder can therefore never OUTRUN the rotation — it converges to mirroring it, upward and downward — meaning pillar 1's NBA-phase delivery ("Score thirty a night and the role follows", docs/CAREER.md:39-41) is bounded by the franchise's own ability-driven minutes: you cannot score thirty in the minutes the ladder cannot win you. The acceptance gate (career-acceptance.ts:200) honestly checks the clock is answered, but in the NBA an "answer" moves nothing engine-visible unless the player exploits the widened plan. C13 registers "the ladder drives grading/trust/phone" as the cost; it does not register that the ladder is thereby also causally inert on its own climb.
Breaks: the register's completeness (C13 understates); the immersion claim's felt delivery in the mode's longest phase.

## LOW packages/career/src/nbabridge.ts:388-389 - Stale defect comment: the careerControlled skip it reports missing has landed
The comment says "defect reported: ai/fa.ts runAiOffseasonDecisions lacks the careerControlled skip"; franchise/src/ai/fa.ts:400 has exactly that skip (and the rollover pass has its twin, franchise/src/tick.ts:413). The transaction branch it annotates is now dead-in-practice for player options. Misleading provenance comment (AGENTS §5).

## LOW packages/career/src/nbabridge.ts:501-510 - A same-day trade can produce an un-carded but card-graded night
`playsToday` is decided from the pre-day `career.nbaTeam` before `advanceDay`; franchise trades execute in the AI block BEFORE games (franchise/src/tick.ts day order). If the old team is idle and the new team plays that day, the game sims with my RAW tendencies (no approach swap) yet `gradeMyGames` — running after `reactToTransactions` updated `career.nbaTeam` — grades adherence against the card the engine never saw, and the legacy path burns `nextApproach` on it (trust.ts:129-130). One night per trade at most; violates week.ts's stated contract that "what the engine simulates is exactly what every grade judges" (week.ts:230-232) across the trade seam.

## LOW packages/career/src/nbabridge.ts:594-607,674 - Offer terms churn weekly while expiresWeek advertises a six-week window
Offers are recomputed per (year, week) with fresh jitters and club picks; `expiresWeek = week + offerWindowWeeks` (6) is displayed but never checked by `applyNbaOffer`/`applyAbroadOffer` (acceptance just re-finds the id in THIS week's build, nbabridge.ts:783). Accepting an offer a week after seeing it signs at the new week's money/club, not the displayed terms. Deterministic and replay-safe, but the surfaced window is cosmetic and the terms are not stable across it.

## LOW packages/career/src/nbabridge.ts:148-153,217-221 - My market prices off a flat group mean while the league prices off the position blend
`overallOf` is the unweighted mean of six group means; the market it claims to mirror uses the position-weighted `abilityScore` (ai/fa.ts:88-92 via ai/roster.ts:74-78 / ai/valuation.ts:81-91). The comment concedes the barrel gap and asserts it "tracks it closely" — unquantified; for position-skewed builds the divergence shifts `fairAavOf` and, more importantly, the `nbaMarketFloor` gate (nbabridge.ts:612) that decides two-way-only vs real offers and the descent doors. stock.ts:69 already deep-imports `positionBlend` from franchise src, so the barrel constraint is not actually binding practice in this package.

## LOW packages/career/src/nbabridge.ts:832 - The signing event's "(X role promised)" text is a load-bearing format with no trap comment at the write site
phone.ts:544 parses `/ \((\w+) role promised\)/` out of this event to run the promise/grievance tracker (phone.ts:512-545 documents the dependency; the writer does not). Rewording the event silently kills the grievance thread. AGENTS §5: traps get called out where they live.

## LOW packages/career/src/nbabridge.ts:606 - Offer aging runs on the league clock while docs put my age on the career clock
`age = league.season - me.bornSeason` drives `yearsForAge` and the `ABROAD_AGE` door; week.ts:168 computes my age as `career.clock.year - me.bornSeason`. The two drift ~+1 league year per 7 career years (docs/CAREER_INTERNALS.md "The two clocks": "my age runs on the career clock"), so late-career the market ages me 1-2 years faster than the career's own age: shorter offers and abroad doors arrive early. Defensible as "the market reads the league's books", but it contradicts the internals sentence and can flip the 27/33 term bands for the same player between the two modules.

## LOW packages/career/src/tick.ts:181 + packages/career/src/nbabridge.ts:858 - draftPrep is a purgatory phase: an undrafted player can neither retire nor take the abroad doors his own market shows him
`buildMyOffers` shows abroad offers to a thin-interest undrafted FA, but `applyAbroadOffer` denies outside phase 'nba' (nbabridge.ts:858) and `retire` denies outside nba/china (tick.ts:181); `transitionAtYearWrap` has no draftPrep branch, so the only exit is accepting an NBA/two-way offer. An exit always exists (the below-floor path guarantees one two-way call), so no hard lock — but the UI offers doors the state machine refuses, and docs/CAREER.md:328-331 ("Retirement is chosen, not imposed, unless the offers genuinely stop") has no imposition mechanism in the NBA phase either (only the once-per-season fade event, nbabridge.ts:466-474).

## LOW packages/career/src/nbabridge.ts:335-342 - The delta reconcile loses in-day tendency deltas at clamp saturation; "loss-free" is overbroad
If the card saturates a tendency (e.g. usage ≥ 68 with assertiveness 100 clamps projected usage to 100, approach.ts:84), an in-day franchise delta to that same tendency — the midseason review's +1 usage (people/dev.ts:272-274) — is absorbed by the clamp on the projected object, reads as delta 0 against `tendBase`, and never lands on the original. Same shape for attr under `playingHurt` (review growth computed against the −8 sheet re-applies a slightly wrong delta). Narrow windows (all-star mark on a my-game day plus saturation), bounded ±1, but the header claims the swap "is loss-free ... both ways" (nbabridge.ts:23) without registering the clamp exception.

## What is done well
- The approach-swap architecture is genuinely careful: shallow-copying only attr/tend so seasons/health/devLog/awards ride shared references is the right cut, the finally-block restore cannot be skipped, and the delta-vs-snapshot reconcile is the correct way to keep the card's own shift out of the fold-back. Stat rows, injuries, and dev notes verifiably land durably (gameday.ts:542-545 pushes into the shared array).
- The module header is a model of convention documentation: decision-id and offer-id grammars, the kind-placeholder honesty ("the 'nba:' id prefix is authoritative"), coach reset rules, the money dedupe intent, and per-stream draw counts are all written where the next agent will look — and the id-prefix law is followed everywhere (tick/UI resolve NBA-ness from prefixes, never kind).
- Fixed-draw discipline in `buildMyOffers` is exemplary: all five draws happen up front "whatever the branch" (nbabridge.ts:595-600), so branch changes cannot reshuffle sibling draws; the offer view is a pure function of state and the tests pin its purity.
- Registered simplifications are mostly honest and cross-referenced: C13/C14/C15 in docs/CAREER.md match the code (no minutes pricing off role, trade requests morale-only, no offer-sheet match window on me), and the careerControlled seam is enforced in all four franchise decision paths that matter (fa market, option passes ×2, retirement rolls).
- Error discipline: every choice seam returns `{ ok, errors }` and wraps franchise executor throws (nbabridge.ts:758-760, 835-837), so a bad id or a moved cap sheet can never crash a career; the minimum-ladder fallback in applyNbaOffer (nbabridge.ts:806-810) handles rooms that moved since the offer printed.
- trust.ts's reacting-world invariant is structurally unconditional in both directions, with the ceiling/floor branches thoughtfully turning the response into trust when no rung exists — and the ladder/par/grading design (role-relative production, the chucker tax with its named note) is a clean piece of game math.
- money.ts really is single-writer: `recordEarning` is the only `ledger.push` in the repo, and every entry names its source.

## Verified sound
- Approach-swap restoration: `league.players[me]` is the original object after every week (test + code path); the card's tendency shift never leaks (delta computed against the post-projection snapshot); scalar fields written by franchise systems during the day (morale, status, contract, rights, twoWayGamesUsed, retiredSeason) are all reconciled by assignment, and every remaining mutable FrPlayer field (potential, disposition, health, seasons, awards, devLog, draft) is a shared reference by construction — enumerated against the FrPlayer shape (franchise types.ts:209-244) and the actual writers (dev, injury, disposition, transactions, gameday).
- Development deltas on my game days: the midseason review mutates projected attr/tend/devLog; attr/tend re-apply by delta exactly once (projected discarded), devLog is shared — no double-application path exists; aging and the offseason review run on the rollover day, which has no games, so they always hit the original.
- Playoff and play-in games are covered by `dayHasMyGame`: `advancePostseason` routes scheduled games into `league.schedule`/`league.playin` (franchise tick.ts:253-265), the two collections the check scans — the approach card is live in the postseason.
- Trades/waives/coach changes mid-week: franchise day order (AI block before games) means a trade day's games sim with post-trade rosters; `reactToTransactions` runs per-day before grading with per-day txStart, re-keys `career.nbaTeam`, resets the coach per the header rules (trade → draft-slot role; same-team coach change → role survives, trust re-earns), and grading then follows the new team — checked against transaction shapes.
- Determinism: no Math.random/Date anywhere in the career package; all nbabridge/epilogue streams (`career-gm-fill`, `career-nba-coach:<teamId>` 1 int, `career-nba-offers:<year>:<week>` 5 calls drawn up front, `career-hof` at most once) match the CAREER_INTERNALS registry; every team iteration in nbabridge sorts keys first; `pickContender` and the room sort break ties deterministically by id; the career and league seeds are distinct namespaces (creation uses a derived world seed), so no stream collisions.
- `ensureAiSeats`/`ensureAiLeague` iteration orders differ (sorted vs insertion), but genesis leaves exactly one null chair and creation.ts:590 fills it at career start, so both fillers are no-ops in real careers; "first filler wins" is registered for the residual case.
- `signingSeasonOf` (nbabridge.ts:206-215) is byte-equivalent to cba/contracts.ts `signingSeason` (verified side by side); capSheet's 3-arg season form and `spaceWithHolds` semantics match the market's usage; the two-way branch prices under my personal minimum correctly, so `applyNbaOffer`'s `offer.money < min` routing is consistent with `buildMyOffers`' construction.
- Contract-window surfacing is once-per-window by stable event id (`ev-contract-<id>` via pushEventOnce), and every stated default path holds: unanswered player options ride as exercised by inaction (both franchise AI passes skip careerControlled player options), a declined/ignored extension flows to the restricted summer, an unanswered QO leaves a restricted FA whom the AI market never signs and never sheets (fa.ts:284 precedes sheet filing).
- The trade-request event-log convention: the 'asked out:'/'trade request withdrawn' prefixes are written nowhere else in the repo; latest-wins scanning is consistent between nbabridge and the office screen.
- The abroad two-maps behavior career-side: `applyAbroadOffer` binds ONE object into both pools, `applyNbaOffer` deletes the career-map entry on the road back, week.ts's `career.players[me] ?? league.players[me]` reads the same object while abroad, and app loadCareer (saves.ts:73-77) rebinds a JSON fork to the league copy — the registered trap holds end to end.
- promiseGraceGames is consumed (phone.ts:493-495, 826) — not dead surface; abroad accrual dedupes once per (career year, label); the china salary clamp [1.5x, 3x] my minimum matches the documented band; the walk-on offer at money 0 accrues nothing by the `money > 0` guard.
