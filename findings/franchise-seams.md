# franchise-seams

scanned: packages/franchise/src/people/dev.ts (364), people/injury.ts (182), people/retire.ts (129), people/disposition.ts (225), tick.ts (958), gameday.ts (607), ai/fa.ts (411), ai/trade.ts (586), ai/roster.ts (276), transactions.ts (304), rng.ts (52), types.ts (careerControlled/waiverWire/health contracts); careerControlled consumers and seam counterparts: packages/career/src/week.ts (302), nbabridge.ts (927), tick.ts (493), stock.ts (enterDraftClass region), circuits.ts (header, projection, jobs, fold, seedBracket), epilogue.ts (status greps), creation.ts (constants); packages/engine/src/core/rng.ts, packages/stats/src/box.ts (pace basis); docs/CAREER_INTERNALS.md, docs/FRANCHISE_INTERNALS.md, docs/CAREER.md, docs/FRANCHISE.md, docs/REGISTER.md (W67); git: 4b3c9af, 684e36e, caab029, 0bbe280
critical: 0   high: 3   medium: 7   low: 7

## HIGH ai/roster.ts:222 - aiRosterUpkeep signs and converts careerControlled players with no skip and no career-side surface
The types.ts:701-705 contract says listed players are never signed "to a decision" by the world, and fa.ts:284 honors it — but ai/roster.ts has no careerControlled check anywhere. Two live paths: (1) the roster-floor fill (roster.ts:222-231) runs every day OUTSIDE moratorium/freeAgency (camp, regular, lottery, draft, postseason) and signs the best minimum free agent from minimumMarket (roster.ts:180-192, no list filter) — a career player who declined his offers, or who is ABROAD (applyAbroadOffer leaves me in league.players as a freeAgent for whole China/Euro seasons, nbabridge.ts:874-885), is at the TOP of that ability-sorted market and gets auto-signed onto an NBA roster the first time any team dips under 14; (2) the two-way conversion (roster.ts:234-264) waives and re-signs the best healthy two-way — reachable for me because buildMyOffers' below-floor tier signs me to a real two-way contract (nbabridge.ts:789-801). The career cannot even see it happen: reactToTransactions (nbabridge.ts:370-398) handles trade/waive/optionDecision/coachChange but has NO 'signing' or 'claim' arm, so path (1) is fully silent, and path (2) surfaces only the waive half — career.nbaTeam goes null while the league plays me every night. In the abroad case I end up simultaneously on an NBA roster and in a circuit, stats folding into the same seasons array from two worlds.
Breaks: the careerControlled seam guarantee (types.ts:701-705, docs/CAREER.md pillar 1); career save coherence (career.nbaTeam/phase vs league roster state diverge silently).

## HIGH career/tick.ts:180 + epilogue.ts - choosing retirement never retires me in the league; the world carries an immortal me forever
applyChoice 'retire' (career/tick.ts:180-187) flips career.clock.phase and builds the epilogue, but nothing career-side ever calls executeRetirement or sets my FrPlayer.status to 'retired' (grep: zero hits in packages/career/src). There is no roster guard either, so retiring mid-contract leaves me status 'roster', playing real games every night of the "retired" phase's season-at-a-time advances (career/tick.ts:426-443). Retiring as a free agent leaves me a permanent league free agent: runRetirements skips me unconditionally (retire.ts:116), the FA market skips me (fa.ts:284), but dev reviews and applyAging keep running on me forever (dev.ts:283-297, 311 — no careerControlled skip, correct for a live career, wrong for a finished one), and the roster-upkeep fill (HIGH above) can sign my retired self back onto a team. CAREER.md:164 promises "after retirement the save stays alive: the league continues, and the GM chair unlocks in the same world" — that world contains an unretirable zombie me.
Breaks: the retirement pillar (CAREER.md:327-328) and the post-career same-save world; almanac/records of every post-retirement season the zombie plays in.

## HIGH career/tick.ts:397 - the euro/nbl year-wrap guard excludes no one: descent veterans become draft prospects and the age-40 ending is unreachable outside china
Commit caab029 claims "the euro year-wrap routes to draftPrep only on the pre-NBA showcase path so a descent veteran abroad is not a prospect, and 40 retires every abroad phase". The implemented guard is `career.nbaTeam === null && !career.epilogue` (career/tick.ts:397) — but applyAbroadOffer sets `career.nbaTeam = null` for every descent (nbabridge.ts:874), so BOTH the showcase teen and the 36-year-old descent vet satisfy it. Consequences: (a) every euro/nbl year wrap returns 'draftPrep' at line 400 before the age check at line 404 can run, so `(phase==='euro'||phase==='nbl') && age>=40` is dead code — only 'china' can ever hit the forced ending; (b) the descent vet is funneled into draftPrep, where resolveDraftNight re-enters him into the draft class (career/tick.ts:466-467, stock.ts enterDraftClass) as a mid-30s prospect; (c) draftPrep has no year-wrap arm at all and the 'retire' choice is denied outside nba/china (career/tick.ts:181), so a player who declines the escape offers loops in draftPrep with no ending of any kind. Franchise-side corroboration: retire.ts:112-116 correctly guarantees the WORLD never ends a controlled career, so nothing backstops these holes — this is the mechanism behind the known soft-lock.
Breaks: a documented guarantee (caab029 commit message + the inline comment at career/tick.ts:398); the euro/nbl/draftPrep career-ending paths.

## MEDIUM career/nbabridge.ts:631 - the "offers genuinely stop" ending has no implementation
CAREER.md:327-328: "Retirement is chosen, not imposed, unless the offers genuinely stop." buildMyOffers structurally cannot stop offering: below the market floor the thinnest-roster team always floats two-way money (nbabridge.ts:631-641 — the sort always yields a team), and thin interest adds both abroad doors (nbabridge.ts:644-672). With retire.ts:116 skipping the hazard, an NBA-phase career is unbounded by construction at any age and any ability — "chosen, not imposed" holds, but the documented exception does not exist anywhere. Together with the HIGH above, no involuntary ending exists outside china.
Breaks: CAREER.md:327-328's design claim; the career-shape acceptance bands' premise that careers end.

## MEDIUM career/week.ts:219 + franchise/injury.ts:167 - abroad and post-entry draftPrep injuries recover at double speed
Post-entry, me is the SAME object in career.players and league.players (applyAbroadOffer nbabridge.ts:885; enterDraftClass for draftPrep). A circuit-phase week runs resolveWeek's recovery clock (-7 days/week, week.ts:219-225) AND tick.ts fastDays (career/tick.ts:472-474) advances the franchise league 7 days, where advanceRecoveries decrements the same injury.remainingDays once per day (injury.ts:167-182, no status filter). Net: -14 days per week for china/euro/draftPrep injuries, halving outDaysBySeverity semantics exactly where the career rolls its own injuries (rollMyInjury). Pre-entry phases are single-ticked (I am not in league.players) and the NBA phase is franchise-owned only — the doubling is specific to the dual-pool phases.
Breaks: the injury out-time calibration (params.injury.outDaysBySeverity, REAL-ish research 05 D6 ranges) in abroad/draftPrep phases.

## MEDIUM career/nbabridge.ts:353 - the energy economy has no mechanical teeth in the NBA phase
CAREER.md:180: "energy debt raises injury hazard through the existing model" — unqualified. In the NBA phase (most of a career): injuries are rolled by franchise rollPostGameInjuries, which knows nothing of career.energy (the energyLowHazardMult multiplication exists only in the circuit-side roll, week.ts:172-174; nbabridge.ts:26-27 states no career-side rolls, correctly avoiding double jeopardy), AND advanceMyGameDay projects me with applyApproach only — applyLegs is never applied (nbabridge.ts:353-367; contrast circuits.ts:630-631 which applies card then legs). So post-entry, energy affects training banks and event copy but neither hazard nor game-night attributes. Not in the W67 registered-simplification list (REGISTER.md:200).
Breaks: CAREER.md:180's claim; the week economy's stakes in the phase that dominates a career (the allocation economy still charges energy it never cashes).

## MEDIUM franchise/people/disposition.ts:183 - the weekly disposition recompute erases the career's morale writes
updateDispositions recomputes morale as a PURE function of league state for every rostered player, me included (disposition.ts:183-186, no careerControlled consideration), on the spine's 7-day cadence (franchise tick.ts:869-871). The career treats my morale as an accumulator: the life slot adds lifeMoraleGain (week.ts:144) and a trade request costs requestMoraleCost (nbabridge.ts:918-924). Every career-side delta is wiped at the next pulse, so in the NBA phase the 'life' allocation and the request's morale price are placebos lasting at most 7 league days. Two ownership models writing one field; the pure recompute always wins.
Breaks: the career allocation economy's honesty (a slot that charges energy for an effect the world erases); the stated consequence of setTradeRequest.

## MEDIUM docs/CAREER_INTERNALS.md:99 - the careerControlled skip list overstates coverage (RFA auto-match, trade AI)
The seams section claims LISTED players are skipped by "RFA auto-match, and the trade AI when the user chair is human". Neither is list-keyed: resolveExpiredOfferSheets (franchise tick.ts:232-250) has no careerControlled check — a pending sheet on a listed player WOULD auto-match-sign him; the actual protection is upstream (fa.ts:284 never files a sheet on a listed player, and '/api/action' 409s under a career mount, app/server.ts:550). trade.ts contains zero careerControlled references — its human-chair gate (trade.ts:553) keys on league.userTeam's gm being null, not on the list, and in career mode every chair is persona-run so listed players are tradeable by design (surfaced via reactToTransactions). The frozen types contract (types.ts:701-705) and the 684e36e commit message both name only the three real skips (retirement, player options, FA market); the doc's two extra items describe gm-null conjuncts as if they were list semantics, and one of them (auto-match) is a missing defense-in-depth check.
Breaks: the seam's documented contract for any future writer of offerSheets (a hand-built save or new AI path silently signs a controlled player).

## MEDIUM docs/CAREER_INTERNALS.md:107 - the reviewPlayerDevelopment extraction's stated consumer does not exist; circuit players (the rival included) never develop
The seams row says the per-player extraction exists "so career training rides the same growth model the league uses", and caab029's parent commit says circuits "develop players through the same curves". reviewPlayerDevelopment is never called outside dev.ts's own loop (grep: dev.ts:169/287 and the barrel export only). Career training rides only distributeGrowth/groupMean (week.ts:103) — a banking economy, not the review model — and NOTHING develops circuit players pre-entry: the rival, generated at 17 to be tracked "for fifteen years" (creation.ts:497-519), keeps his creation-day dials through 4+ years of HS/college circuits until enterDraftClass hands him to the league. The extraction itself is sound and RNG-order preserving (verified against the 684e36e diff); its stated purpose is unwired.
Breaks: CAREER_INTERNALS.md:107-110's claim; the rival arc and circuit-world development premise (draft boards evaluate a statically-aged class against my trained player).

## MEDIUM career/week.ts:197 - career-side wear writes ignore the 0-100 contract the franchise side enforces
types.ts:122-123 contracts wear as "0-100 accumulated career wear". Franchise assignment clamps (injury.ts:154 `Math.min(100, ...)`); the career's two writers do not: rollMyInjury adds wearBySeverity raw (week.ts:197) and playing hurt adds playHurtWearMult*0.5 per game (week.ts:253). Wear above 100 extrapolates every consumer past its documented semantics: hazardFor/rollMyInjury wearF beyond wearFactorAt100 (injury.ts:93, week.ts:171), applyAging wearF beyond wearDeclineFactorAt100 (dev.ts:321), and retireHazardFor's wear term beyond its stated 1.75x-at-100 (retire.ts:59, moot for me but the object can re-enter world math).
Breaks: the health contract at a seam boundary; wear-driven aging/hazard calibration for a play-hurt-heavy career.

## LOW career/nbabridge.ts:389 - stale "defect reported" comment: the skip it reports missing was added in the same commit
reactToTransactions' player-option arm says "defect reported: ai/fa.ts runAiOffseasonDecisions lacks the careerControlled skip" — commit caab029 itself added that skip (fa.ts:400), and the rollover backstop has its own (franchise tick.ts:413). The branch's explanation ("the franchise AI pass decided over my head") describes a now-unreachable path.
Breaks: comment truth (AGENTS §2.10: the comment now contradicts the code it explains).

## LOW franchise/types.ts:685 - waiverWire is dead surface with no STAGED/UNWIRED label; claimWaiver can never succeed
league.waiverWire has no writer anywhere (executeWaive is immediate-FA per register X1, transactions.ts:9-12); its only reader is the claimWaiver action gate (tick.ts:669), which therefore always denies. Neither the field (types.ts:685) nor the action carries a STAGED/UNWIRED label.
Breaks: AGENTS §2.5 (unlabeled dead surface); a UI offering an action that cannot ever validate.

## LOW docs/CAREER_INTERNALS.md:85 - stream registry drift: career-injury key, career-train semantics, bracket stream name
(1) Registry line 86 lists `career-injury:<year>:<week>`; the code keys per game — `career-injury:<gameId>` (week.ts:177), a deliberate doubleheader fix documented in week.ts:9-11. (2) Line 85 and the week.ts table row (line 36, "probabilistic integer training landings at the calibrated rate") predate the deterministic pity-timer: `career-train` is reserved and draws nothing (week.ts:8-9, 25-33). (3) circuits.ts:17 names the bracket stream `career-circuit:<year>:bracket` while the caller and registry use `career-bracket:<year>` (week.ts:268, CAREER_INTERNALS.md:83) — draws nothing today, naming drift only.
Breaks: the stream registry's authority (rng.ts doctrine: the registry is how collisions are prevented by construction).

## LOW docs/FRANCHISE.md:196 - disposition claimed to influence "retirement timing"; the hazard takes no disposition input
FRANCHISE.md §5 says ambition/loyalty/professionalism "influence contract choices, trade requests, and retirement timing". RetireFactors (retire.ts:31-42) is age/minutes/wear/decliningSeasons/contractYears — no disposition term. FRANCHISE.md:210-211's own model description ("age, decline, role, and wear") matches the code; line 196 does not.
Breaks: doc-vs-code (a reader tuning retirement via disposition would be tuning nothing).

## LOW career/week.ts:175 - a healthy player who declares playingHurt skips the injury roll entirely
rollMyInjury returns at line 164 when an injury is listed, which fully covers the stated rationale ("playing hurt wears, it does not re-roll"). The second guard at line 175 is therefore only reachable when the card says playingHurt while HEALTHY — and it grants injury immunity for that game. Self-punishing (dulled sheet via applyApproach, extra wear at week.ts:251-253) but backwards: the flag meant to model risk-taking removes the risk. No draw-order impact (the per-game stream is untouched entirely).
Breaks: hazard-exposure honesty for one card configuration.

## LOW franchise/gameday.ts:102 - full-results scans per projection: quadratic passes over the season
trailingLoadMinutes (gameday.ts:102-115) and playedOn (gameday.ts:88-95) iterate ALL of league.results per player / per team on every game day; results grow to ~1300 records per season, so late-season days do O(records x rostered-players) work for a fixed 7-day window. Same family: fa.ts marketClass (fa.ts:104-108) scans the full, never-pruned transactions ledger every market day, across multi-decade saves. Bounded (server-side, seconds per season) but pure waste against a per-day index.
Breaks: nothing functional; perf budget on long saves (brief axis 6).

## LOW franchise/gameday.ts:468 - "per-48" pace comment is wrong for rule-pack jobs
foldEvents' lite-totals comment says "box.pace is already the per-team per-48 possession figure"; with GameJob.rules the basis is the pack's own regulation (periods x periodMinutes — per-32 for prep, per-40 for FIBA/NCAA), which is exactly what line 437 sets up and what the career wants. Code right, comment NBA-only; a circuit-side consumer reading the comment would mis-normalize.
Breaks: comment truth at the one seam where the basis varies.

## What is done well

- The careerControlled seam, where implemented, is disciplined and honest: retire.ts:112-116 documents the draw-shift consequence of skipping a listed player on the shared stream instead of hiding it; fa.ts:284 skips before any draw so the day stream stays stable within a save; the player-option skip exists in BOTH the deadline pass (fa.ts:400) and the rollover backstop (tick.ts:413), with team options deliberately left to the club and the reasoning stated at each site.
- The reviewPlayerDevelopment extraction really is RNG-order preserving: per-player 'dev:<season>:<playerId>' streams make the hoist bit-exact, the 684e36e diff shows the body moved with the draw sequence untouched, and the commit pinned it with the autosim determinism test.
- distributeGrowth/distributeDecline carry the rounding remainder down the group so integer writes preserve the group total to within half a point, and the delta-clamped-to-headroom call convention plus integer ceilings make per-dial ceiling overshoot impossible — a tight little piece of math that holds under both franchise and career callers.
- Draw-order discipline in the dev review is exemplary: noise drawn unconditionally per group before any gate can continue, and the offseason breakout roll deliberately decorrelates the two same-season reviews on the shared stream — both documented at the point of confusion.
- The GameJob.rules passthrough is complete where it counts: pace folds to the pack's regulation with actual game minutes (OT included) in the denominator, OT counts derive from folded periods against the pack, H-prefix labels key on the pack's period count, and the clutch window respects pack regulation.
- Injury severity is rolled before kind, so a bad night can never inflate a day-to-day sprain into an ACL; already-out players never re-roll; the catalog's REAL weights cite their research rows and the FEEL fills say so.
- runRetirements mutates nothing and returns sorted ids for the spine's single executor — clean write-ownership, pinned by a test that also asserts the non-mutation.
- gameday projection keeps the depth chart on TRUE pre-debuff ability so a road night never reshuffles who a team thinks its best players are, and the last-resort promotion beats every rest/scratch policy — both traps called out in comments where they live.

## Verified sound

- distributeGrowth: carry-chain preserves the group total (|error| <= 0.5); with integer ceilings (gen.ts:345, creation.ts:446 both Math.round) and delta <= group headroom, no dial can exceed the ceiling (per-dial step <= its own headroom, proof checked for n >= 2); the career pity-timer's whole=1 landings satisfy the same precondition (mean < ceiling implies an integer weight >= 1 >= delta).
- The pace fold is OT-correct: stats box.ts:443-454 scales actual gameMinutes (event-derived, OT included) to opts.paceMinutes; gameday.ts:437 passes periods x periodMinutes; franchise NBA jobs default to 48.
- clockLabel/keyPlays honor the pack: H prefix iff regulation periods == 2 (PREP 4x8 correctly labels Q), OT/OT2 labeling after pack regulation, late-clock lead changes gated on period >= pack regulation.
- hazardFor cannot exceed its cap: worst plausible factor product (48 min, age 40, proneness 100, wear 100) ~= 0.147, far under the 0.95 guard; the career-side twin clamps to 0.5 at the roll; units (per-36 exposure, linear in minutes) are self-consistent with the 12-14 games-missed CAL target.
- The retire cohort pin is honest math: hand-computed hazard-implied median for the test's 600-minute/wear-20 player is 34, inside the 33-38 assertion; seasonMinutes=600 correctly does NOT take the fringe boost (< 600 strict); 2+ contract years multiply hazard by exactly 0.15.
- Enumerated every franchise auto-decision touching a player and its seam status: retirement rolls (skipped, retire.ts:116), FA market signings/camp deals/offer-sheet filings (skipped as a block, fa.ts:284), AI player options (skipped, fa.ts:400 + tick.ts:413), team options (deliberately the club's, surfaced via reactToTransactions), QO tenders (deliberately the club's; the restricted summer is surfaced as the qo: decision window), trades (deliberately allowed; surfaced + coach re-keyed), draft-squeeze waives (surfaced via the waive arm), rotation/minutes (deliberately franchise-owned, REGISTER W67a), extensions (no AI extension path exists at all). The two unskipped-AND-unsurfaced paths found are the roster-upkeep fill/conversion (HIGH #1).
- No double injury jeopardy in the NBA phase: nbabridge does no career-side rolls (header nbabridge.ts:25-27 matches the code); the franchise post-game roll is the only roll on my real minutes.
- Determinism of the streams these systems use for career players: dev:<season>:<playerId> and injury:<season>:<day> under the league seed post-entry, career-injury:<gameId>, career-nba-offers (5 fixed calls drawn up front whatever the branch), career-nba-coach (1 int); all registered or header-documented; iteration orders sorted or genesis-stable; the Box-Muller pair cache cannot vary draw counts here because every stream is freshly derived per call site and dev noise draws unconditionally.
- rollPostGameInjuries consumes the per-day stream in applyGameResults' index-sorted record order; planDayJobs sorts by game id — replay-stable end to end.
- '/api/action' 409s under a career mount (app/server.ts:550), bounding the missing RFA auto-match defense-in-depth to hand-built states.
- The reconcileSwap loss-free claim holds for its enumerated fields: shared seasons/health/devLog/awards references, delta-reconciled attr/tend (card shift excluded via the post-projection snapshot), scalar assignment for morale/status/contract/rights — including a mid-day dev review or trade landing on the projected copy.
